# Version (`-v` / `--version`)

Printing the CLI version uses the **root** Commander option (not a subcommand):

```bash
i18nprune --version
i18nprune -v
```

Implemented in **`bin/cli.ts`** (`.version(...)`).
