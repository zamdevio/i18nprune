# remix-routes stack fixture

Remix-style `app/routes/*.tsx` modules with `t()`; `locale_directory` + `locale_per_dir` under `locales/`.

```bash
cd tests/fixtures/stacks/remix-routes
node ../../../../dist/cli.js validate --json
```
