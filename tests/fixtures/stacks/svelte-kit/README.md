# svelte-kit stack fixture

Svelte `.svelte` files with `t()` and `locale_directory` + `locale_per_dir` locale layout.

```bash
cd tests/fixtures/stacks/svelte-kit
node ../../../../dist/cli.js validate --json
```
