# nuxt-vue stack fixture

Nuxt-like `src/pages/` and `src/components/` Vue SFCs with `locale_directory` + `locale_per_dir` (`locales/<code>/messages.json`).

```bash
cd tests/fixtures/stacks/nuxt-vue
node ../../../../dist/cli.js validate --json
```
