# react-tsx

Minimal **React** tree: `t()` in `.tsx` (Remix / Vite React shaped).

```bash
cd tests/fixtures/stacks/react-tsx
node ../../../../dist/cli.js config --json
node ../../../../dist/cli.js doctor --json
node ../../../../dist/cli.js validate --json
```
