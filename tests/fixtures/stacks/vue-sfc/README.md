# vue-sfc

Minimal **Vue SFC** tree: `t()` in `<script setup>` (Nuxt / SvelteKit-shaped extractor path for `.vue`).

```bash
cd tests/fixtures/stacks/vue-sfc
node ../../../../dist/cli.js config --json
node ../../../../dist/cli.js doctor --json
node ../../../../dist/cli.js validate --json
```
