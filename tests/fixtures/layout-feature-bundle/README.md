# layout-feature-bundle

Minimal fixture: **`locale_directory`** + **`feature_bundle`** (`messages/<feature>/<locale>.json`).

```bash
cd tests/fixtures/layout-feature-bundle
node ../../../dist/cli.js locales list --json
```
