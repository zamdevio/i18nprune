# `locales list`

Summaries of which locale files exist under **`localesDir`**, path/key counts, and alignment hints (implementation in progress; will align with **`review`** and **`validate`**).

```bash
i18nprune locales list
```

[← Back to `locales`](../README.md)
