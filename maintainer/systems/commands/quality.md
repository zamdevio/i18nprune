# `quality` — command system sheet

## Status

- **Stability:** stable
- **Last reviewed:** systems tree bootstrap

## CLI entry

- **`runQuality`:** `packages/cli/src/commands/quality/jsonEnvelope.ts`
- **Human:** `packages/cli/src/commands/quality/run.ts`

## Core & shared

- Quality / parity checks — `packages/core/src/quality/` and related (see imports).

## Frozen API

- **`--json`** quality payload; stable issue codes (**e.g.** `english_identical_leaves`).
