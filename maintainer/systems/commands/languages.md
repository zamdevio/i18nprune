# `languages` — command system sheet

## Status

- **Stability:** stable
- **Last reviewed:** systems tree bootstrap

## CLI entry

- **`runLanguages`:** `packages/cli/src/commands/languages/jsonEnvelope.ts`
- **Human:** `packages/cli/src/commands/languages/run.ts`

## Core & shared

- Language catalog / RTL hints from `@i18nprune/core` (see envelope).

## Frozen API

- **`--json`** languages list payload.
