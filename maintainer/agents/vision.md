# Agent context: vision

i18nprune targets **correctness first**: validate keys, sync locale shape, and generate locales with clear **`--json`** and human modes. Preserve **`RunOptions`**; use **`logger`** for user-facing output; keep commands **non-interactive-safe** when stdin is not a TTY.
