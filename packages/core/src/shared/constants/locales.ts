/** Maximum directory depth under `locales.directory` when walking locale JSON segments. */
export const MAX_LOCALE_SEGMENT_TREE_DEPTH = 16;

/** Keep locale target suggestions helpful without turning typo warnings into noisy catalog dumps. */
export const MAX_LOCALE_TARGET_SUGGESTIONS = 3;
