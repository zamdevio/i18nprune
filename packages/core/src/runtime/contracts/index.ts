/**
 * Runtime host contracts only (ports + adapter shape).
 */
export type {
  ConfigPathSystemRuntime,
  CoreEngineRuntime,
  ProjectFilesystemRuntime,
  RuntimeAdapters,
  RuntimeDirEntry,
  RuntimeFsCap,
  RuntimeFsPort,
  RuntimeKind,
  RuntimeNetworkCap,
  RuntimeNetworkPort,
  RuntimePathCap,
  RuntimePathPort,
  RuntimeReadFsPort,
  RuntimeSystemCap,
  RuntimeSystemPort,
} from '../../types/runtime/index.js';
