/**
 * Synchronous helpers over injected `RuntimeFsPort` (assert sync, read JSON, list dir).
 * Import from `@i18nprune/core/runtime/helpers/sync` so bundlers can split this surface from async adapters.
 */
export * from '../../helpers/sync/index.js';
