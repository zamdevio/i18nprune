/**
 * Cross-operation types (e.g. `run.*`). Import via **`@i18nprune/core/types`** (re-exported there)
 * or **`@i18nprune/core/types/shared`** when you want only this barrel.
 */
export type * from './run/index.js';
export type { CompiledScanExclude } from '../scanner/compile.js';
