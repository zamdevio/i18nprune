/** Roll-up counts for `locales dynamic` grouping (E.6c). */
export type DynamicSiteGroups = {
  mixedConstRuntime: number;
  templateInterpolation: number;
  nonLiteral: number;
  emptyCall: number;
  commented: number;
};
