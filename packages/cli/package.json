{
  "name": "@i18nprune/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CLI binary and programmatic API source for i18nprune. Private workspace package; the installable npm release is built from the repository root.",
  "scripts": {
    "typecheck": "tsc --noEmit --project ../../tsconfig.json"
  },
  "dependencies": {
    "@i18nprune/core": "workspace:*",
    "@i18nprune/report-schema": "workspace:*"
  }
}
