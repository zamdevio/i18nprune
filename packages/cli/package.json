{
  "name": "@i18nprune/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CLI source for i18nprune (workspace-only). Published npm package is the repository root `i18nprune@0.1.0`; keep this version aligned with root and @i18nprune/core.",
  "scripts": {
    "typecheck": "tsc --noEmit --project ../../tsconfig.json"
  },
  "dependencies": {
    "@i18nprune/core": "workspace:*"
  },
  "exports": {
    "./constants": {
      "types": "./src/constants/index.ts",
      "default": "./src/constants/index.ts"
    }
  }
}
