/**
 * CLI identity for the marketing site — same module as `packages/cli` (`CLI_NAME` / `CLI_VERSION`).
 */
export {
  CLI_NAME,
  CLI_VERSION,
  CLI_VERSION as SITE_VERSION,
} from "../../../../packages/cli/src/constants/cli";
