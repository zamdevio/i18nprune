/**
 * Report error surfaces (see `main.tsx` and `context/report/index.tsx`):
 * - `GlobalErrorSurface` + `RootErrorBoundary` from `./root.js` — window + React tree
 * - `PayloadErrorScreen` from `./payload.js` — missing / JSON / Zod validation
 */
export {};
