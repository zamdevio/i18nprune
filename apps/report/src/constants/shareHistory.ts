/** Default max share-history rows kept in localStorage. */
export const SHARE_HISTORY_DEFAULT_MAX = 999;

/** Hard cap for user-configured max history (Settings). */
export const SHARE_HISTORY_HARD_MAX = 9999;

export const SHARE_HISTORY_EXPORT_SCHEMA_VERSION = 1 as const;
