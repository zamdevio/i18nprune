/** @deprecated Import from `../open-in-editor/index.js` — thin re-export for existing imports. */
export {
  EDITOR_OPENER_OPTIONS,
  type EditorOpener,
  getStoredEditorOpener,
  setStoredEditorOpener,
  inferRuntimeFamily,
  type GeneratorRuntimeFamily as RuntimeFamily,
} from '../open-in-editor/index.js';
