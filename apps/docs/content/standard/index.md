# Standards (placeholder)

Status: Placeholder

This folder is reserved for long-lived, policy-style standards once phase work stabilizes.

Current source of truth for execution is:

- `maintainer/phases/standard-toolkit.md`

Planned future contents (post-hardening):

- Cross-surface parity standard (CLI / JSON / report / core exports)
- Issue-code lifecycle standard (constant + emit + docs + links)
- "Start in 60 seconds" onboarding standard
- Edge-case documentation standard (`solved` vs `unsolved`)
