# `quality` (`q`)

**Full examples:** [quality examples](../../examples/commands/quality)

Reports **English-identical** and parity-style metrics using config **policies** where implemented.

```bash
i18nprune quality
i18nprune quality --target ja
```
